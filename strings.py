# the '\' character acts as an escape character
print("hello\nworld")

phrase = "lorem ipsum lorem"
print(phrase.upper())

# get the length of a string
print(len(phrase))

# get the first character, use the index
print(phrase[0])

print(phrase.index("o"))

# replace something in a string
print(phrase.replace("lorem", "siomai"))